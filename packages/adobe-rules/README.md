# Adobe Rules

Planned deterministic implementation of `docs/ADOBE-RULESET.md`.

Every marketplace rule requires an official source, retrieval date, version, evaluator type, severity/blocking behavior, and safe synthetic fixture. Rules run against a specific revision/checksum and cannot directly grant acceptance guarantees. Rule changes require evidence, tests, and synchronized ruleset documentation.