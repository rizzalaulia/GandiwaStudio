# UI Package

Planned accessible React components derived from `docs/DESIGN.md`.

Shared MVP components include content/format selectors, backend and worker health, queue/job status, audit verdicts, artifact-expiry notice, Adobe-ready gate, and actionable redacted errors. Components must not imply a hard-coded healthy state or guarantee Adobe Stock acceptance.