# Provider SDK

Planned connector interface for user-selected endpoints/models. This package must not implement cross-provider routing, fallback, load balancing, combo ordering, or automatic provider selection.
