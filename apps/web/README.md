# Web Application

Planned React + TypeScript browser workspace. The browser owns File System Access API directory handles and writes returned artifacts to the portable project folder. Application code will be scaffolded only after the documentation baseline is approved.
